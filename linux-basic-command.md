# command to become a super or main admin or root user in the machine
sudo su-
#identify the present working directory
pwd
# list the files in the folder
ls
# list the files in the desending order based on timestamp
ls -lrt
# create directory
mkdir
#to change directory
cd
